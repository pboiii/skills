interface:
  display_name: "Goal Setup"
  short_description: "Create verifiable /goal execution packets"
  default_prompt: "Use $goal-setup to decide whether this task warrants goal mode, create only the applicable goal-packet files, add runnable checks when useful, and return a launch command under 4,000 characters. Do not start the goal unless I explicitly ask."
